<div class="container">
  <h1>Tasks</h1>

  <h4><%= pluralize @tasks.count, "task" %></h4>
  <div class="task-cards-wrapper">
    <% @tasks.each do |task| %>
      <%= render "task", task: task %>
    <% end %>
    <br>
  </div>
</div>
